{
  "name": "monday-bot-vercel",
  "version": "0.1.0",
  "description": "monday.com webhook bot — posts triage comments, triggers Cursor agents",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/functions": "^1.5.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}
