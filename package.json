{
  "name": "set-bot",
  "version": "0.1.0",
  "description": "Set — a monday.com bot that triages tickets and dispatches Cursor background agents",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/functions": "^1.5.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}
