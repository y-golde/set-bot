{
  "name": "set-bot",
  "version": "0.1.0",
  "description": "Set — a pluggable ticket-bot that dispatches coding agents (Monday/Jira tickets × Cursor/Anthropic agents)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/functions": "^1.5.0"
  },
  "scripts": {
    "dev": "vercel dev"
  }
}
